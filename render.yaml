services:
  - type: web
    name: mehmetzekicicekweb-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: SETUP_KEY
        sync: false